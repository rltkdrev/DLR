<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>과학실 예약 - 동화고등학교</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/fullcalendar@5.10.1/main.min.css" rel="stylesheet">
    <style>
        .login-container {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f8f9fa;
        }
        .login-box {
            background: white;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 400px;
            width: 90%;
        }
        .login-box h2 {
            margin-bottom: 1.5rem;
            color: #333;
            font-size: 1.8rem;
            line-height: 1.4;
        }
        .login-box p {
            color: #666;
            margin-bottom: 1.5rem;
        }
        .google-btn {
            background-color: #4285f4;
            color: white;
            padding: 12px 24px;
            border-radius: 5px;
            text-decoration: none;
            display: inline-block;
            transition: background-color 0.3s;
            font-size: 1.1rem;
            font-weight: 500;
            border: none;
            cursor: pointer;
        }
        .google-btn:hover {
            background-color: #357abd;
            color: white;
            text-decoration: none;
        }
        .school-logo {
            max-width: 250px;
            margin-bottom: 2rem;
            width: 100%;
            height: auto;
        }
        .calendar-container {
            margin: 20px;
        }
        .reservation-list {
            margin: 20px;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        .error-message {
            color: #dc3545;
            margin-bottom: 15px;
            font-weight: bold;
            display: none;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="login-box">
            <img src="images/school-logo.png" alt="동화고등학교 로고" class="school-logo">
            <h2>과학실 예약 시스템</h2>
            <div id="errorMessage" class="error-message">동화고등학교 계정(@donghwa.hs.kr)만 로그인 가능합니다.</div>
            <p class="text-muted">※ 동화고등학교 계정만 사용 가능합니다.</p>
            <a href="/auth/google" class="google-btn">
                <i class="fab fa-google"></i> 로그인
            </a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script>
        // URL 파라미터 확인
        const urlParams = new URLSearchParams(window.location.search);
        const error = urlParams.get('error');
        
        // 도메인 오류가 있을 경우 오류 메시지 표시
        if (error === 'domain') {
            const errorElement = document.getElementById('errorMessage');
            errorElement.style.display = 'block';
            
            // 오류 파라미터 제거 (페이지 새로고침 시 메시지가 계속 표시되지 않도록)
            window.history.replaceState({}, document.title, '/');
            
            // 팝업 표시
            setTimeout(() => {
                alert('동화고등학교 계정(@donghwa.hs.kr)만 로그인 가능합니다.');
            }, 500);
        }
    </script>
</body>
</html> 